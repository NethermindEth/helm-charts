# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""
namespaceOverride: ""

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []

# existingSecret -- REQUIRED. Name of existing secret containing sensitive configuration
# Use external secret management (e.g., Infisical, External Secrets Operator)
# See examples/infisical-secret.yaml for a complete example
# The secret MUST contain the following keys:
#   - CB_JWT_ETHGAS_COMMIT: JWT token for CommitBoost authentication
#   - CB_JWTS: JWT tokens in format "ETHGAS_COMMIT=<token>"
#   - CB_SIGNER_JWT: JWT token for signer
#   If useJwtAuth = true:
#     - ACCESS_JWT: ETHGas Exchange access token
#     - REFRESH_JWT: ETHGas Exchange refresh token
#   If useJwtAuth = false:
#     - EOA_SIGNING_KEY: EOA private key for transactions
existingSecret: ""

# existingDirkSecret -- Optional. Name of existing secret for Dirk TLS certificates
# Only required if using Dirk as remote signer (configured via ethgas.signer)
# If using web3signer or other remote signers, leave empty
# The secret must contain (base64 encoded):
#   - ca.crt: CA certificate for Dirk TLS
#   - dirk.crt: Client certificate for Dirk TLS
#   - dirk.key: Client private key for Dirk TLS
existingDirkSecret: ""

image:
  initContainer:
    repository: alpine
    pullPolicy: IfNotPresent
    tag: "3.12"
  pbs:
    repository: ghcr.io/commit-boost/pbs
    pullPolicy: IfNotPresent
    tag: "v0.8.0"
  signer:
    repository: ghcr.io/commit-boost/signer
    pullPolicy: IfNotPresent
    tag: "v0.8.0"
  ethgas_commit:
    repository: ghcr.io/ethgas-developer/commitboost_ethgas_commit
    pullPolicy: IfNotPresent
    tag: "v1.0.0-beta.7"

ethgas:
  network: "Hoodi"
  rpc_url: "https://ethereum-hoodi-rpc.publicnode.com"
  relays: "https://0xb20c3fe59db9c3655088839ef3d972878d182eb745afd8abb1dd2abf6c14f93cd5934ed4446a5fe1ba039e2bc0cf1011@hoodi-relay.ethgas.com"
  entity_name: ""
  exchange_api_base: "https://hoodi.app.ethgas.com"
  collateral_contract: "0xe8bfB84b14c383b94365a895fc8bfA36dE236dc8"

  # useJwtAuth -- Set to true to use pre-generated JWT tokens (ACCESS_JWT, REFRESH_JWT)
  # Set to false to use EOA signing key for automatic registration
  # Note: All sensitive values MUST be provided via existingSecret
  useJwtAuth: false

  # Remote signer configuration
  # Example for Dirk (uncomment and modify):
  # signer: |
  #   [signer.dirk]
  #   cert_path = "/signer/secrets/dirk.crt"
  #   key_path = "/signer/secrets/dirk.key"
  #   ca_cert_path = "/signer/secrets/ca.crt"
  #
  #   [[signer.dirk.hosts]]
  #   url = "https://dirk-0.example.com:8881"
  #   wallets = ["Validators"]
  #
  # Example for Web3Signer (uncomment and modify):
  # signer: |
  #   [signer.remote]
  #   url = "http://web3signer-url:6174"
  signer: ""

# Extra arguments to pass to the ethgas binary.
extraArgs: []

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port:
    pbs:
      http: 18550
      metrics: 10000
    signer:
      http: 20000
      metrics: 10001
    ethgas_commit:
      metrics: 10002

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
