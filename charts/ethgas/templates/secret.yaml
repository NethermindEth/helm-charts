{{- if not .Values.existingSecret }}
{{- fail "existingSecret is required. Please create a secret using Infisical or another secret management system. See examples/infisical-secret.yaml for reference." }}
{{- end }}
