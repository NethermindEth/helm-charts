apiVersion: v1
kind: Service
metadata:
  name: {{ include "arbitrum-token-bridge.fullname" . }}
  labels:
    {{- include "arbitrum-token-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arbitrum-token-bridge.selectorLabels" . | nindent 4 }}